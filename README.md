# Todo CLI

A simple command line todo manager with priorities, due dates, and persistence.

## Usage

### Add a Todo
```bash
python main.py --add "Your todo title"
```

### Remove a Todo
```bash
python main.py --remove TODO_ID
```

### List Todos
```bash
python main.py --list
```

## Todos are saved to ~/.todos.json
